# Max — X Content Agent
# DRY_RUN=true until Jordan creates X account and approves first batch
DRY_RUN=true

# X API Credentials (Jordan creates account -> add keys here)
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=

# X API tier: "free" or "basic" ($200/mo — needs Jordan's approval)
X_API_TIER=free
